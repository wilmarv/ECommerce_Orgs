import { ImageURISource } from "react-native";

interface Detalhes {
    nome: string,
    logoFazenda: ImageURISource,
    nomeFazenda: string,
    descricao: string,
    preco: string,
    botao: string,
};
export default Detalhes;